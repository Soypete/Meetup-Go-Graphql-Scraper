# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib
*.swp

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

.secrets
dev/
*.csv
# secrets file for connecting to meetup
*.json
*.db
*.wal

dist/
